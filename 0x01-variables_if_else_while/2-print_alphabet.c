#include <stdio.h>
/**
 * main - open sesemane point
 *
 * Return: Always 0 (congrats/great)
 */
int main(void)
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
putchar(alphabet);
}
putchar('\n');
return (0);
}
